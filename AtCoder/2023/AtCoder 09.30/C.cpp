#include <bits/stdc++.h>
using namespace std;

int main(){
    int n, m; cin >> n >> m;
    vector<bool> fogos(n);
    while(m--){
        int temp; cin >> temp;
        fogos[temp-1] = true;
    }
    int f = 0
    while(!fogos[f]){
        count++;
        f++;
    }
    while(count--){
        cout << 
    }
}